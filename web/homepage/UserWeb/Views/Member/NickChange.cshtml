
@{
    Layout = "~/Views/Shared/_PopupLayout.cshtml";
}
<style type="text/css">
    <!--
    body {
        margin: 0;
        font-size: .7em;
        font-family: Verdana, Arial, Helvetica, sans-serif;
        background: #666;
    }

    h1 {
        font-size: 2.4em;
        margin: 0;
        color: #FFF;
    }

    h2 {
        font-size: 1.7em;
        margin: 0;
        color: #FFF;
    }

    h3 {
        font-size: 1.2em;
        margin: 10px 0 0 0;
        color: #000;
    }

    #header {
        width: 96%;
        height: 50px;
        margin: 0 0 0 0;
        padding: 0 2% 0 2%;
        font-family: "trebuchet MS", Verdana, sans-serif;
        color: #FFF;
        background: linear-gradient( to bottom, #000, #666);
    }

    #content {
        position: relative;
        padding: 5px;
    }

    .content-container {
        background: #eee;
        width: 95%;
        padding: 10px 10px 20px 10px;
        position: relative;
        text-align: center;
    }
    -->
</style>
<div id="header"><div style="padding-top:11px;"><h2>닉네임변경</h2></div></div>
<div id="content">
    <div class="content-container">
        <div style="text-align:left;color:#333;font-size:9pt;">
            <form name="form" method="post" action="@Url.Action("UpdateNickname")">
                현재닉네임 : <b>@ViewBag.nickname</b><br />
                @if (ViewBag.CanChange)
                {
                    <text>
                        신규닉네임 : <input type="text" name="v_nick" size="20" value="" />
                        <a href="#" onclick="$(this).closest('form').submit()" class="btn btn-sunflower" style="width:80px;height:17px;font-size:13px; padding: 1px 1px 1px 1px;">닉네임변경</a>
                        <br />
                        (한글, 숫자 2~7글자만 가능)<br />
                    </text>
                }
                else
                {
                    <text>
                        <b>@ViewBag.nickname</b> 님께서는 이번달 2회 닉네임 변경을 하셨습니다.<br />
                    </text>
                }
            </form>
        </div>
    </div>
</div>